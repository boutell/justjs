<% if (permissions.post) { %>
	<div class="btn-group">
		<a class="btn btn-primary btn-large" href="/new">New Post</a>
	</div>
<% } %>

<ul class="posts">
<% _.each(posts, function(post) { print(options.templates.postBody(post)); }) %>
</ul>
